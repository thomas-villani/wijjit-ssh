<!--
The changelog is maintained at the repository root, because that is where a
contributor editing it expects to find it and where GitHub renders it. This page
is a thin wrapper so the docs site does not carry a second, drifting copy.

MyST is enabled in conf.py precisely for this include.
-->

```{include} ../../CHANGELOG.md
```
